(function() {
    // IIFE to calculate the square of a number
    const number = 7;
    const squareResult = number * number;

    // Display the result immediately
    console.log(`The square of ${number} is: ${squareResult}`);
})();
